# Measurement IDs. Public client IDs, not secrets. Leave unset locally so dev
# visits are never counted; on Netlify set them for the production context only.
# PUBLIC_POSTHOG_KEY must be the same PostHog project the map app uses.
PUBLIC_POSTHOG_KEY=
PUBLIC_GA_ID=
